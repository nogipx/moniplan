name: monishare
description: MoniShare v1 transport service for planner collaboration.
version: 0.1.0
publish_to: "none"

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  collection: ^1.18.0
  rpc_dart: ^2.2.2

dev_dependencies:
  test: ^1.25.0
